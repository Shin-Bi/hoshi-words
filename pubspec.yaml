name: tsuki_words
description: 별빛 단어 UI와 학습 흐름을 체험하는 자체 작성 데이터 데모.
publish_to: none
version: 0.1.0+1
environment:
  sdk: ^3.11.0
dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.3
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
flutter:
  uses-material-design: true
  assets:
    - assets/data/
